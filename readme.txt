=a http 'agent' for mega.nz

pip install pycurl

==downland a file 
mega.py https://mega.nz/#!(8-byte id)!(43-byte key)

==run as http server

run httpd.py

http://server_ip:port/?!(8-byte id)!(43-byte key)

suppot http-range 
so you can downland mega.nz via aira2c

==ipv6

set USING_IPV6 in .py
